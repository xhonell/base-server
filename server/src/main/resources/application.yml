# application.yml
server:
  port: 8802

spring:
  application:
    name: SERVER-SERVICE-01
  profiles:
    # 环境标识
    active: DEV

  config:
    import:
      - optional:nacos:COMMON-SERVICE-${spring.profiles.active}?group=COMMON_GROUP
      - optional:nacos:CONFIG-SERVICE-${spring.profiles.active}?group=COMMON_GROUP

  cloud:
    nacos:
      # 注册中心
      discovery:
        server-addr: 124.223.100.39:8848
        username: "nacos"
        password: "12345678"

      #配置中心
      config:
        import-check:
          enabled: false
        server-addr: 124.223.100.39:8848
        group: COMMON_GROUP
        file-extension: yaml
        refresh-enabled: true
        namespace: ""
        username: "nacos"
        password: "12345678"
